<%= form_for(prodorderfeedback) do |f| %>
  <% if prodorderfeedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prodorderfeedback.errors.count, "error") %> prohibited this prodorderfeedback from being saved:</h2>

      <ul>
      <% prodorderfeedback.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :productorder_id %>
    <%= f.number_field :productorder_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :feedbacktxt %>
    <%= f.text_area :feedbacktxt %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
